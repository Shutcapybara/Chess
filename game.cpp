/*
#include <vector>
#include <memory>
#include "Pieces/piece.h"
class game
{
private:
    std::vector<std::vector<std::unique_ptr<Piece>>> gameBoard;

public:
    game(int boardHeight = 8, int boardWidth = 8)
        : gameBoard(boardHeight, std::vector<std::unique_ptr<Piece>>(boardWidth))
    {
    }
};
*/